{
  "platform": "TABIRUN_EVENT_BUILDER",
  "version": "1.0.1",
  "baseline": {
    "live_event_platform": "2.6.0",
    "map_core": "tabirun_map_system_editor_v20_privacy_operations.html",
    "policy": "freeze existing working modules and add an editor/orchestration layer"
  },
  "change_policy": [
    "Do not rebuild the whole app to fix one module.",
    "Freeze working modules and replace only the module under change.",
    "Every release declares baseline, changed modules, unchanged modules and regression checks.",
    "Do not remove or rename an existing data path without a migration plan.",
    "Presets are recommendations only and must never permanently remove capabilities.",
    "Unknown future fields and classifications must be addable without rewriting neighboring modules.",
    "For every generated release intended for GitHub Pages, always provide the exact upload filename(s) and the expected post-upload GitHub Pages URL(s) in the same response."
  ],
  "taxonomy": {
    "model": "main_category > sub_category > field",
    "ui": "expandable tree",
    "data_driven": true,
    "custom_main_categories": true,
    "custom_sub_categories": true,
    "custom_fields": true,
    "custom_field_types": [
      "text",
      "textarea",
      "number",
      "date",
      "datetime-local",
      "checkbox",
      "select"
    ]
  },
  "modules": {
    "map": {
      "baseline": "v20",
      "frozen": true
    },
    "participant": {
      "baseline": "foundation-1.0",
      "frozen": true
    },
    "schedule": {
      "baseline": "foundation-1.0",
      "frozen": true
    },
    "liveLocation": {
      "baseline": "privacy-20.0",
      "frozen": true
    },
    "chatSupport": {
      "baseline": "foundation-1.0",
      "frozen": true
    },
    "privacyGuard": {
      "baseline": "20.0",
      "frozen": true
    },
    "questAdapter": {
      "baseline": "adapter-1.0",
      "frozen": false
    },
    "publication": {
      "baseline": "builder-1.0",
      "frozen": false
    }
  },
  "map_bridge": {
    "purpose": "reuse v20 without rebuilding it",
    "legacy_storage_key": "tabirun_map_block_editor_v19_current",
    "per_event_copy": "event.mapBridge.mapBlock",
    "safety": "backup the current v20 map block before handing another event to v20"
  },
  "persistence": {
    "builder": "IndexedDB tabirun_event_builder_v1",
    "runtime": "unchanged from LIVE_EVENT_PLATFORM foundation",
    "note": "builder event definitions are separated from runtime participant/LIVE/CHAT posts"
  },
  "regression_requirements": [
    "v20 MAP CORE file is not modified by this release",
    "existing LIVE_EVENT_PLATFORM core data paths are not renamed",
    "route remains optional",
    "CHAT remains independent from MAP",
    "QUEST remains an adapter",
    "new event does not automatically inherit the previous event map",
    "current v20 map block is backed up before handoff",
    "custom fields do not mutate built-in field identifiers"
  ],
  "release_output_policy": {
    "required": true,
    "rules": [
      "Every generated HTML/JSON release must include the exact filename to upload.",
      "Every response that delivers a release must include the expected GitHub Pages URL after upload.",
      "If multiple companion files are required, list every filename and every corresponding post-upload URL.",
      "Never describe a GitHub Pages URL as live until repository upload succeeds and Pages has refreshed."
    ],
    "pages_base": "https://nuritakug-cpu.github.io/tabirun-bgm/"
  }
}